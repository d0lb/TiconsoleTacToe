#include <stdio.h>
#include "board.h"
#include <stdbool.h>

bool isRunning = true;
char board[3][3] ={{'X', 'O', 'X'}};

int num1, num2;

int main(){
    printf("Welcome to multiplayer console TicTacToe Game!");
    while (isRunning){
        printf("Starting the game... 
            \n You both will have to enter two numbers,
            that means coordinates of a TicTacToe board
            (Example: 2 2 --- makes a symbol 
            in the center) \n");
            while(1){
                printf("X's turn!");
                scanf
            }
    }

}